#!/usr/bin/env node
path = require('path');

exports.command = {
  description: 'Interact with FreshBooks Projects',
  arguments: '-l [--debug]'
};

if (require.main === module) {
  require(path.join(__dirname, '..', 'index.js'));
}
